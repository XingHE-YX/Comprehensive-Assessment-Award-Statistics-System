# 综测成果申报系统 PRD

版本：v1.0
日期：2026-09-07
状态：开发基线

## 1. 产品愿景

为单个班级提供一个手机优先的综合素质测评成果申报工具，替代腾讯问卷完成成果收集、证明材料归档、人工审核、分值核定和 Excel 汇总。系统支持多个学年，但一次只开放一个学年；学生不注册账号，通过班级口令进入，每项成果独立提交并获得可修改凭证。

## 2. 用户与场景

### 学生

学生使用手机或电脑完成以下任务：查看当前学年与日期要求、提交一项成果或无材料声明、上传证明材料、保存申报编号和修改码、查询审核状态、按管理员要求补充材料。

### 超级管理员

班委或系统维护者使用后台完成：查看当前学年申报量、按字段筛选、打开附件、更新审核状态、填写审核备注、核定分值、维护学年和班级口令、导出 Excel。

## 3. 用户故事

1. 作为学生，我可以输入班级口令进入申报页面，以免公开暴露申报入口。
2. 作为学生，我可以选择七种成果类别，页面只显示该类别需要填写的字段。
3. 作为学生，我可以一次提交一项成果并上传至少一个 JPG/JPEG/PNG/PDF 证明文件。
4. 作为学生，我可以提交“本学年无成果”声明，且该声明不要求附件。
5. 作为学生，我可以在成功页看到申报编号和 8-10 位修改码，并在之后查询本条申报。
6. 作为学生，我可以在待审核或需补充材料时修改申报；已通过和不予认定时只能查看。
7. 作为管理员，我可以使用用户名和密码登录后台，未登录者不能访问后台路由。
8. 作为管理员，我可以按学年、姓名、学号、类别和审核状态筛选，并按最新提交时间查看列表。
9. 作为管理员，我可以查看每条申报的类别字段和附件，更新状态、备注和核定分值。
10. 作为管理员，我可以设置唯一当前学年、截止时间、首页说明和班级口令，无需重启应用。
11. 作为管理员，我可以导出包含申报明细和学生汇总的 `.xlsx` 文件，直接用于筛选和统计。

## 4. 功能范围

### P0 功能

- 当前学年展示、有效日期、截止时间、填写须知。
- 班级口令验证和短期学生访问 session。
- 学生姓名、学号、成果/无成果选择。
- 七类成果固定字段：学术科技竞赛、文体比赛、其他获奖表彰、发表文章、社会实践/服务、专利、学习技能/资格证书。
- 条件字段动态显示；所有规则在服务端再次校验。
- 文件上传：JPG、JPEG、PNG、PDF，单文件 ≤10 MiB，每条成果 1-10 个。
- 成果编号格式 `ZCYYYY-000001`，修改码保存 Argon2id 验证哈希和认证加密密文，管理员可在受保护的申报详情中查看。
- 查询、附件访问和学生修改权限。
- 管理员登录、列表筛选、详情、附件查看、审核状态、审核备注、核定分值。
- 学生名单导入（姓名＋学号、Excel 模板、CSV 或粘贴）、校验后创建学年、申报中追加名单。
- 学年新建/编辑/激活/删除、首页说明、班级口令修改。
- 全部审核状态及无材料声明的单条/批量删除、回收站恢复，统计和导出排除已删除记录。
- Excel 两张工作表：`申报明细`、`学生汇总`。
- tracing 日志、中文 403/404/500 页面、Docker Compose、Caddy HTTPS、备份脚本。

### P1 功能

后台分页、筛选结果导出、学生修改历史提示增强、图片预览增强、CSV 备选导出、磁盘占用显示、简单登录失败限速。

### P2 功能

多班级、多管理员、自动计分规则引擎、规则配置中心、审核历史日志、消息通知、对象存储。

## 5. 七类成果字段

所有成果共有字段：成果名称（必填）、取得日期（必填）、成果类别（必填）、详细说明（可选）、备注（可选）、附件（至少 1 个）。类别字段保存到 `category_data` JSON，导出时展开。

1. 学术、科技类竞赛：竞赛完整名称；学科竞赛类别（A+/A、B、C、未列入/不清楚）；目录序号（可选）；竞赛级别（国家、省部、市、校）；获奖等级（一等奖、二等奖、三等奖、优秀/鼓励奖、其他）；选择其他时填写实际奖项/名次。
2. 文体类比赛：比赛完整名称；级别（国家、省部、市、校、其他/不清楚）；是否有明确奖项等级；有则填写等级，无则填写实际名次；是否属于东南大学运动会相关项目。
3. 其他获奖表彰：荣誉完整名称；表彰级别；是否奖学金/助学金/奖学金性质（是、否、不确定）。校级荣誉类别为校级时选填，预置优秀团务工作者、魅力团支书、优秀共青团干部、优秀共青团员、五四奖章、其他校级荣誉；旧自填文本可继续查看和编辑。非校级不要求此项。选择“是”时提示需人工确认，不自动拒绝。
4. 发表文章：标题；文章性质。学术论文填写发表类型、作者排序、期刊名称；非学术文章填写平台、发表形式、链接或发表信息。
5. 社会实践/服务：项目名称；级别；本人身份；获奖等级或无具体等级。
6. 专利：名称；类型；状态；项目组排名；专利号或申请号。
7. 学习技能/资格证书：证书/考试类型；CET-4/CET-6；计算机专业类别与考试等级；雅思/托福成绩；其他资格证书名称。证书新选项和成绩标签均为 `CET-4/CET-6`，继续使用 `cet6_score` 字段并兼容旧 `CET-6` 数据；成绩导出为数值单元格。条件字段只在对应类型下出现并校验。

## 6. 业务规则

- 新库启动不自动创建学年。先导入姓名和学号，校验通过后才能创建学年；名单与学年在同一事务中保存，新学年未激活。已有空名单学年保留，激活前须补充名单。
- 每个学年的学号唯一，姓名与学号必须同时匹配，适用于成果、无材料声明及学生修改；校验在服务端持有数据库写锁期间执行。班级口令仍是入口验证，名单不作为本人认证。
- 名单使用“姓名”“学号”两列，支持 `.xlsx`、UTF-8 CSV 或制表符粘贴；学号须使用文本以保留前导零。每次最多 2 MiB、5000 行；相同记录去重，姓名冲突则整批回滚。开放后可继续追加，不覆盖原名单，不自动延长截止时间。
- 全部审核状态的成果和无材料声明都可删除，每次最多 500 条，经确认后进入回收站。删除后学生查询、修改和附件访问失效，列表、计数、总分和后续导出排除这些记录；附件保留以支持恢复。恢复保留审核信息，同一无材料声明已重新提交时整批恢复失败并提示冲突。
- 删除整个学年须输入完整学年名称并明确确认，将永久清理其全部名单、成果、无材料声明及附件（含回收站），不影响其他学年和系统设置。删除当前学年后申报关闭，可重新创建同名学年。文件删除失败进入持久重试队列，在后续清理或启动时重试。
- 学生端只允许向 `is_active=true` 的学年提交；无当前学年时显示未开放。
- `obtained_date` 必须在 `start_date` 和 `end_date` 闭区间内；配置 `deadline` 后，截止时间之后禁止新提交，但管理员仍可查看和修改设置。
- 一次提交只包含一项成果；多项成果必须重复提交。
- 无材料声明写入 `student_declarations`，不创建 submission，不需要附件；同一姓名+学号后续仍可提交成果。
- 无 JavaScript 时可先更新申报选择、类别与条件填写项，保留文本后再选择附件、正式提交；更新填写项本身不保存记录或附件，仍验证 CSRF 和访问权限。首页截止时间明确标注 UTC。
- 审核状态：`pending` 待审核、`approved` 已通过、`needs_revision` 需补充材料、`rejected` 不予认定。
- 学生可编辑 `pending` 和 `needs_revision`；`approved`、`rejected` 只读。学生修改后状态回到 `pending`，保留审核备注并标记重新提交。
- `approved` 建议要求核定分值非空；分值允许 0，最多两位小数；`rejected` 可填 0。
- 编号全局唯一且不直接暴露数据库自增 ID；修改码明文仅在一次性成功页和受保护的管理员详情显示，不进入学生普通详情、列表、Excel、日志或 URL。
- 旧记录只存哈希，原修改码不能恢复；保留原码可继续使用。管理员可通过明确确认的 POST/CSRF 表单手动生成新码，禁止升级时自动重置。重置使旧码和已验证的学生会话失效，申报内容、审核状态/备注/分值及附件保留。
- 修改码密文由持久 SESSION_SECRET 保护并绑定申报编号；需安全备份密钥，轮换需迁移密文。密钥不匹配、密文损坏或遗失时安全显示不可读取，不把哈希当作修改码。

## 7. 成功标准

MVP 必须满足以下全部条件：

- 手机和桌面浏览器均能完成从口令进入到提交成功的流程。
- 错误口令、缺字段、日期越界、无附件、非法类型和超大文件均被服务端拒绝并显示中文提示。
- 正确编号+修改码只可访问对应申报；错误凭证不能查看或下载附件。
- 四种审核状态和学生修改权限准确执行。
- 管理员可筛选、查看附件、审核、填备注和分值。
- Excel 中文无乱码，明细一条一行，类别字段独立列，学生汇总只统计已通过分值。
- Docker 重启、服务器重启后数据库和上传文件不丢失；每日备份保留至少 7 份数据库副本。
- 日志不包含密码、班级口令、修改码、session secret 或内部绝对路径。

## 8. 非目标

本版本不做学生账号体系、短信/微信登录、名单真实性校验、多班级、多租户、多管理员角色、自动识别证书、自动判分、通用表单设计器、消息推送、Redis、PostgreSQL、对象存储、SPA 前端和完整备份控制台。

## 9. 验收数据

验收至少准备：一个当前学年、一个历史学年、七类各一条合法样例、一个无材料声明、四种审核状态各一条、JPG/PNG/PDF 各一个附件、一个非法扩展名文件、一个超过 10 MiB 文件、同一学生多条成果。使用这些数据执行 `APP_FLOW.md` 的主流程和错误流程。
