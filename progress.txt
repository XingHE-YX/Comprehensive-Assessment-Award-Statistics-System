Project: 综测成果申报系统（单班级版）
Last updated: 2026-09-11

Final acceptance: PASSED for the local P0 release (2026-09-10, application commit aa41e71). F1/F2 are fixed with durable HTTP/browser regressions. The requested UI, declaration-list, administrator edit-code and CET changes are complete. Public production-domain TLS remains an actual-deployment check; bounded upload buffering remains documented deviation D2. See docs/final-acceptance.md for current signoff and the preserved historical review.

Current status:
- Project specification documents have been created.
- AGENT.md has been created as the project-level AI execution guide.
- IMPLEMENTATION_PLAN.md section 0 (开工约束) is complete: the Cargo binary package, prescribed directories, exact direct dependencies, and Cargo.lock are initialized.
- Rust toolchain is fixed to 1.88.0 through rust-toolchain.toml.
- The initial project baseline was committed and pushed to `origin/main` at commit `7833487` (`chore: initialize comprehensive assessment system`).
- Task 1 production prerequisite is complete as part of Task 10: validated configuration, configured router/storage/admin session, real listener, migration/seed, health readiness, safe HTML errors and graceful shutdown are connected. The separate historical task1-scaffold worktree is preserved.
- Task 2 is complete: SQLite migrations, domain models, repositories, foreign-key/unique constraints, default academic-year seed, and startup database initialization are implemented.
- Task 3 is complete: Argon2id secret helpers, random edit/submission identifiers, scoped sessions with expiry, CSRF helpers, secure private cookies, protected attachment storage, path/type/size/count validation, and cleanup on batch failure are implemented.
- Task 4 is complete: seven-category schemas/shared validation, optional school-only honor presets and legacy text compatibility pass; final-acceptance F2 is closed.
- Task 5 is complete: home/access/submission/declaration/receipt and the no-JavaScript server-rendered field refresh pass; final-acceptance F1 is closed.
- Task 6 is complete: query credentials, single-submission verified sessions, detail/edit pages, atomic student updates, existing/new attachment limits, protected attachment responses, shared forms, and browser regression coverage are implemented.
- Task 7 is complete: configured administrator login/logout, protected dashboard/detail/review routes, literal keyword and enum/year filters, filter-aware counts, safe attachment previews, score/status/note validation, and responsive admin pages are implemented.
- Task 8 is complete: administrator academic-year creation/editing/atomic activation, immediate class-code replacement, shared validation, responsive settings forms and history-preserving submission/storage integration are implemented.
- Task 9 is complete: protected two-sheet XLSX downloads, 34 expanded detail columns, 10 summary columns, current/historical/all-year and dashboard filters, approved-only totals, attachment identifiers, Excel formatting and compatibility fallbacks are implemented and verified.
- Task 10 is complete: safe application/proxy logs, strict startup/hash validation, request limits and Chinese errors, fixed-version containers/HTTPS, paired backups and recovery, production documentation and acceptance checks are implemented and verified.

Completed documents:
- PRD.md
- APP_FLOW.md
- TECH_STACK.md
- FRONTEND_GUIDELINES.md
- BACKEND_STRUCTURE.md
- IMPLEMENTATION_PLAN.md
- AGENT.md

Next implementation step:
- User manual testing can continue on the roster/recycle local preview. The active 2026-2027 local-test year is newly created with three synthetic roster entries and no submissions. The 2025-2026 inactive year and all configuration are preserved. Details: docs/testing-roster-recycle.md.

Verification:
- `cargo +1.88.0 fmt --check` passed.
- `cargo +1.88.0 check` passed.
- `cargo +1.88.0 clippy --all-targets --all-features -- -D warnings` passed.
- `cargo +1.88.0 test --test db` passed (4 tests).
- `cargo +1.88.0 test --all-targets` passed.
- `DATABASE_URL=sqlite::memory: cargo run` passed; migrations and default seed initialized one active year.
- Cargo.toml direct dependencies were compared with TECH_STACK.md and use exact versions.
- `cargo +1.88.0 fmt --check` passed after Task 3 changes.
- `cargo +1.88.0 test --test auth --test storage` passed (7 tests).
- `cargo +1.88.0 clippy --all-targets --all-features -- -D warnings` passed after Task 3 changes.
- `cargo +1.88.0 test --all-targets` passed after Task 3 changes.
- `cargo +1.88.0 test --all-targets` passed after Task 4 changes (15 tests).
- `cargo +1.88.0 fmt --check` passed after Task 4 changes.
- `cargo +1.88.0 clippy --all-targets --all-features -- -D warnings` passed after Task 4 changes.
- `cargo +1.88.0 test --test submission_flow` passed (5 tests).
- `cargo +1.88.0 test --all-targets` passed after Task 5 changes (20 tests across the existing suite; submission-flow coverage contains 5 tests).
- `cargo +1.88.0 clippy --all-targets --all-features -- -D warnings` passed after Task 5 changes.
- `cargo +1.88.0 fmt --check` passed after Task 5 changes.
- `cargo +1.88.0 fmt && cargo +1.88.0 fmt --check && cargo +1.88.0 test --all-targets && cargo +1.88.0 clippy --all-targets --all-features -- -D warnings && git diff --check` passed after final Task 5 verification; submission-flow coverage includes deadline rejection and distinct sequential submission numbers.
- Final receipt-session ordering fix revalidated with `cargo +1.88.0 fmt`, `cargo +1.88.0 test --all-targets`, `cargo +1.88.0 clippy --all-targets --all-features -- -D warnings`, and `git diff --check`.

Notes:
- Do not add P1/P2 features before P0 acceptance is complete.
- Keep this file free of credentials, access codes, edit codes, session secrets, and personal data.
- Empty required directories contain .gitkeep so the project structure survives Git checkout.
- The GitHub remote is `https://github.com/XingHE-YX/Comprehensive-Assessment-Award-Statistics-System.git`.
- Task 5 uses a public `routes::build_router` with a development session key for integration tests; production startup still needs to supply the configured session secret when Task 1 startup wiring is completed.
- The previous session left no Task 6 changes on disk. This session resumed from clean main at 5c1832f, reproduced the missing /query route as 404, and implemented Task 6.
- Task 6 follows PRD section 6: the deadline restricts new submissions; pending/needs_revision updates use their original academic year even after deadline/deactivation. APP_FLOW.md and BACKEND_STRUCTURE.md now spell out this interpretation.
- Task 6 verification: query_flow has 12 passing tests, auth has 4 passing tests, including expiry/replacement of verified scope; the full Rust suite has 33 tests. cargo fmt --check and strict Clippy pass.
- Browser verification passed using Playwright 1.52.0 with installed Chrome at 320x568, 390x844, 768x1024 and 1440x900. Covered submit, receipt, query, prefilled edit without new files, protected download, no-material declaration, all seven categories, both article branches and all certificate branches. Desktop/mobile screenshots were inspected and no horizontal overflow or overlap was found.
- Read-only code review identified two regressions, both reproduced and fixed: blank optional sports choice and repeated Edit clicks after collapsing the editor.
- Browser test dependencies are installed outside the repository. Re-run instructions are in docs/testing-student-flow.md.
- docker compose config could not run: Docker is not installed on this machine, and Compose files remain part of Task 10. This is not a completed deployment verification.
- A temporary local preview is available through cargo run --example student_preview, using an isolated in-memory database and temporary uploads. It does not wire the production main entry point.

Task 7 delivery notes (2026-09-09):
- Implemented /admin/login, POST /admin/logout, /admin, /admin/submissions/{id}, and POST review, using shared sessions/CSRF and protected attachment routes.
- Added build_router_with_config for configured credentials, session key/cookie policy and body limit. The old build_router is student-only development/test wiring and has no administrator credentials. Production main wiring remains the separate Task 1 reconciliation; this task does not claim deployment readiness.
- Followed APP_FLOW.md over the conflicting backend filter sentence: invalid values are ignored with a visible notice; backend documentation now agrees. Omitted year selects the active year; explicit empty year selects all; no active year preserves historical visibility.
- Counts and approved totals use selected submissions. Declaration counts use only year/name/student-number; the UI explains that distinction. Settings and export controls remain disabled until Tasks 8 and 9.
- Administrator details share the student category-display mapping, show the resubmission marker, and preserve the record if an attachment is unavailable. Review validates all four states, requires Approved score, accepts zero/two decimals, and rejects exponent notation, negative/non-finite/excess-precision scores and overlong notes before transactional updates.
- Read-only review identified default Axum extraction errors. Reproduced and fixed route-local Path/Form/Query rejection handling; anonymous requests still hit the guard first. Visual review found negative zero for an empty approved total; reproduced with an integration test and fixed.
- Added isolated admin preview and repeatable Playwright browser coverage; see docs/testing-admin-flow.md. No production credentials, data or runtime dependencies are introduced.
- Final Task 7 verification passed: cargo +1.88.0 fmt --check; cargo +1.88.0 test --test admin_flow (8 tests); cargo +1.88.0 clippy --all-targets --all-features -- -D warnings; cargo +1.88.0 test --all-targets (41 tests); git diff --check.
- Final Playwright 1.52.0 admin and student browser regressions passed at 320x568, 390x844, 768x1024 and 1440x900. Admin coverage includes JS-disabled tablet forms, PNG/PDF authorization, score errors, needs_revision -> student amendment -> approved, student read-only result and logout. Student regression covers all seven categories and conditional branches. Mobile and desktop screenshots were inspected; no page overflow or overlapping controls was found.
- docker compose config was attempted again and could not execute (Docker command not found). Compose/deployment files remain Task 10, and no deployment claim is made.


Task 8 delivery notes (2026-09-10):
- Implemented GET /admin/settings (optional edit selection), POST /admin/years, POST /admin/years/{id}, POST /admin/years/{id}/activate and POST /admin/settings/class-code. All settings writes require administrator session and CSRF; malformed inputs return safe Chinese errors. The shared admin navigation now enables Settings.
- New years are inactive; editing never changes activation. Activation holds the SQLite write lock, closes other active years, activates the target and commits atomically. Missing targets roll back, and database lock conflicts produce an inline 409. Historical submissions, reviews, declarations and attachments are retained.
- Shared server validation covers unique trimmed year names, canonical dates and ordering, optional UTC deadlines and announcement limits. Native datetime-local inputs explicitly use UTC; RFC 3339 offsets normalize to UTC. Deadlines can be in the past or outside the achievement range, or cleared. The editor retains ordinary values on failure. Class-code values preserve exact input, reject blank/control/overlong values, hash on a blocking worker, save transactionally and never echo the code or hash.
- Fixed two reproduced integration defects: declarations now respect the new-submission deadline, and submission numbers allocate by the public ending-year prefix across all academic-year records so identical ending years do not collide.
- Independent review reproduced and led to two additional fixes: new attachments use immutable year-id directories instead of editable display names, and both new-submission branches reload/validate settings under their persistence write lock after multipart parsing. This prevents settings changes during slow uploads from being bypassed. Existing editable submissions retain the PRD exception to activation/deadline restrictions.
- Added deterministic real-HTTP concurrency coverage with Expect: 100-continue, actual SQLite lock/conflict and rollback checks, code replacement checks, historical query/attachment tests, rename-plus-upload/amendment checks, and cold storage lookup of both current and legacy directories. No dependencies or schema changes were needed; old uploads require no migration.
- Final application checks passed: cargo +1.88.0 fmt --check; cargo +1.88.0 test --test settings (11 tests); cargo +1.88.0 clippy --all-targets --all-features -- -D warnings; cargo +1.88.0 test --all-targets (52 tests); cargo +1.88.0 build --examples; git diff --check. The isolated task worktree used the existing root target directory through CARGO_TARGET_DIR.
- Final Playwright 1.52.0 settings, administrator-review and student regressions all passed at 320x568, 390x844, 768x1024 and 1440x900. Tablet administrator forms worked with JavaScript disabled. Covered settings errors/success, cutoff clearing, old/new code access, historical records and protected files; the student suite also covered all seven categories and conditional branches. Desktop/mobile screenshots were inspected, with no page overflow or overlapping controls.
- Independent scoped re-review found no remaining issues; it separately passed settings and storage checks. Reproduced application errors and the E0425 compilation error/fix are recorded in lessons.md.
- APP_FLOW.md and BACKEND_STRUCTURE.md now describe the precise settings forms, UTC policy, transaction checks and stable attachment layout. Both plan documents mark Task 8 steps complete; repeatable browser instructions are in docs/testing-admin-flow.md.
- docker compose config was attempted and returned command not found (127). Docker is still absent, Compose files remain Task 10, and deployment is unverified. Export downloads remain Task 9; this task preserves and verifies the historical data that export will consume. Production main still needs Task 1 reconciliation.

Task 9 delivery notes (2026-09-10):
- Implemented administrator-only GET /admin/export.xlsx, current-year navigation and a filtered export action beside the dashboard table. The filtered link binds the displayed year explicitly; active-year changes cannot silently change its year scope. Shared parameter validation and bound SQL predicates match the list; invalid export filters return to the dashboard's visible notice.
- The service reads academic years, selected submissions, declarations and attachment metadata in one database snapshot, then generates the workbook on a blocking worker. No schema changes or new runtime dependencies were needed. Python 3 standard-library ZIP/XML inspection is documented as a development test prerequisite.
- The fixed 34-column Detail sheet expands all seven categories and their conditional branches; the 10-column Summary groups the union of submissions/declarations by student number plus name and includes only Approved scores in totals. Declaration filters use year/name/number only; all-year export merges matching student identities across selected years. Headers, dates, numeric scores, leading-zero identifiers, filters, frozen rows and column widths follow the specified contract. Category fields without a dedicated fixed column remain Chinese labeled prose in Detail.
- Download headers use XLSX MIME, no-store, nosniff, sanitized UTF-8 names and an ASCII fallback. Attachment cells contain original names and protected relative download identifiers; no file contents, storage paths, hashes or credentials are exported. User text never becomes a spreadsheet formula.
- Independent review reproduced overlong student category text and valid pre-1900 dates causing entire exports to fail. Added explicit UTF-16-safe truncation notices with protected complete-original references, ISO-text fallback for early dates, and canonical display of the two accepted legacy award aliases. Original database values remain complete. Regression testing also exposed intermediate score-rounding overflow and non-finite values silently becoming text; safe final rounding and explicit numeric validation now cover both.
- Final Rust checks passed: cargo +1.88.0 fmt --check; cargo +1.88.0 test --test export (12 tests); cargo +1.88.0 test --test export --test query_flow (24 tests); cargo +1.88.0 clippy --all-targets --all-features -- -D warnings; cargo +1.88.0 test --all-targets (64 tests); cargo +1.88.0 build --examples; git diff --check. The isolated task worktree reused the root target directory via CARGO_TARGET_DIR.
- Final Playwright 1.52.0 administrator, settings and student/export regressions passed at 320x568, 390x844, 768x1024 and 1440x900. Tablet administrator forms/downloads work with JavaScript disabled. Tests cover filtered/current/empty/history downloads, renamed years, student amendment followed by approval, logout protection, all seven categories/conditional branches, 17 actual browser submissions and 4 declarations. Mobile export actions stack at full width; mobile/desktop screenshots were inspected.
- LibreOfficeDev headless successfully opened and re-saved seven-category, header-only, approved-filtered and historical workbooks. Independent ZIP/XML comparisons confirmed both sheets, Chinese cell values, numeric data, filter ranges and frozen first rows survived the roundtrip. Host Fontconfig warnings were non-fatal; there is no claim of a Microsoft Excel GUI run.
- Read-only scoped re-review returned clean after the fixes and independently passed all 12 export tests. Both plan documents mark Task 9 complete. Full schema, compatibility boundaries and reproducible browser/workbook checks are in docs/testing-export.md; confirmed build/test/tool issues are recorded in lessons.md.
- docker compose config was attempted and returned command not found (127). Docker remains absent, Compose files belong to Task 10, and deployment remains unverified. This does not block the requested Task 9 implementation or its GitHub delivery to origin/main.

Task 10 delivery notes (2026-09-10):
- Connected the production binary to the existing current application instead of replacing it with the old Task 1 scaffold. Config now validates required values, strict Base64, Argon2id versions/parameters/salt, positive body limits and production Secure cookies. The stdin-only --hash-secret utility creates administrator hashes without exposing passwords as arguments. Dependencies, lockfile, schema and existing UI design tokens are unchanged.
- Added repository-backed /healthz, graceful SIGINT/SIGTERM and pool shutdown, audited JSON events with generated request ids/matched route templates, dependency/SQL logging exclusion, consistent failure delays, actual streamed and declared request-body limits, and shared Chinese HTML errors. Caddy error logs also remove request/trace fields. Cookie, CSRF, sensitive marker and safe error behavior is covered by new health/security tests; existing behavior tests retain their business assertions after the intended HTML error-format change.
- Added Dockerfile, docker-compose.yml (web/caddy only), Caddyfile.example, .env.example, ignored runtime/secret files, readonly application container/root/backup mount, persistent host directories, image health checks and BuildKit caches. Private raw env_file avoids PHC dollar interpolation; only public domain configuration reaches Caddy. README explains initialization, local run, immutable image deployment, hash generation, logs, cron/systemd timers, failure recovery and paired restore.
- backup.sh pauses and confirms stopped web state (including restarting containers), snapshots SQLite committed WAL and matching uploads, checks integrity, publishes atomically and retains at least seven complete pairs. Failures produce controlled nonzero events and attempt to restore original service intent. Lock/partial cleanup failures remain failures. Daily backups use a short maintenance window; sessions require reauthentication after process restart.
- Independent task review found and verified three corrections: reject unsupported Argon2 PHC versions/short salts before startup; stop restarting containers before snapshotting; suppress raw utility errors and propagate cleanup failures. All three passed scoped re-review, with no open Important/Critical issues.
- Final checks passed: cargo +1.88.0 fmt --check; cargo +1.88.0 clippy --all-targets --all-features -- -D warnings; cargo +1.88.0 test --all-targets (74 tests); python3 tests/backup.py; python3 tests/backup_failures.py (eight scenarios); bash -n scripts/backup.sh tests/smoke.sh; git diff --check. No direct dependency or Cargo.lock changes remain.
- Docker is now available for acceptance in a separate Colima profile. Verified Docker Engine/client 27.5.1, Compose 2.35.1, Caddy 2.9.1 and linux/amd64 Rust 1.88.0 release builds. Original Compose configuration passed config --quiet with the normal public/private environment split and /opt/zongce/{data,uploads,backups} mounts. Both services became healthy. Local CA-verified HTTPS returned health ok and HTTP redirected with 308; the real HTTPS smoke completed submission, protected download, query/edit, administrator review and XLSX download.
- Tested real 404/proxy502 requests with synthetic query/header markers; application/Caddy logs contained no credentials, student names, internal paths or markers. Fresh sessions after web restart recovered the same approved record, exact attachment bytes and Excel. A complete host-VM restart also recovered services/data automatically. Nine real automatic backups retained seven pairs; latest-pair restore preserved previous directories and passed the same HTTP checks. Copy fault injection returned nonzero and recovered web without deleting valid backups. A real crash-loop container confirmed the repaired restarting-state backup path and lock cleanup.
- Existing admin/settings/student/seven-category-export browser suites all passed sequentially through a temporary adapter for the real main entry point at 320x568, 390x844, 768x1024 and 1440x900; tablet administrator forms ran without JavaScript. New 403/404 error pages also passed all four sizes. Mobile/desktop screenshots were inspected, with no page overflow or overlapping controls.
- Reusable deployment and verification instructions are in README.md and docs/testing-deployment.md. Actual application/test/tool errors and fixes are in lessons.md. Local HTTPS used a private test CA; no public production-domain certificate or live deployment is claimed. Earlier notes saying main is unwired or Docker absent are historical and superseded by this delivery.
- Final whole-branch review identified the locked Rust driver's bundled SQLite 3.46.0 differing from the required production 3.46.1. Docker now checksum-verifies the official 3.46.1 source and statically links it without changing Cargo dependencies/lockfile. A mandatory release startup gate and the running container's SQLx database_ready.sqlite_version both verified 3.46.1. The new engine passed full HTTPS smoke, existing-data compatibility, safe logs and another paired backup/restore for both old and new records; scoped final review approved the fix. Native Cargo's default bundled engine and exact-version native linkage are explicitly documented in TECH_STACK/README.
- Implementation commits: f80429a (production startup/deployment/backups), 0e1997f (review fixes), 42d4f51 (exact production SQLite linkage/version gate). Final records and GitHub delivery are handled in the concluding documentation commit on origin/main.

Final acceptance matrix review (2026-09-10):
- Reviewed all six IMPLEMENTATION_PLAN section 3 areas at 7ad62cf with independent student, admin/export and security/deployment source reviews. The requested review is complete; the final acceptance result is NOT PASSED because F1/F2 remain open. No application source was changed in this review.
- Fresh cargo +1.88.0 fmt --check, cargo +1.88.0 test --all-targets (74), strict Clippy, both backup Python checks (including eight fault scenarios), shell syntax, Compose config --quiet and diff checks passed.
- Existing admin/settings/export-plus-student browser suites ran sequentially against temporary actual amd64 image instances with SQLite 3.46.1, passing all four required sizes. The normal JS declaration and all seven category positive branches passed; tablet admin forms ran without JS. Screenshot inspection found no page overflow/overlapping controls.
- Separate current-image browser reproduction confirmed no-JS declaration submits zero HTTP requests because confirmation stays hidden and result/date/category/file remain required. A valid national Other Award without school_honor_category returned 422; the visible field is a required INPUT with zero preset options. Both observations are documented as defects, not accepted behavior.
- Independent real HTTP matrix ran 110 cases: 13 valid branch baselines, 48 individual missing category fields and identity/date/length/enum/numeric/CSRF/declaration/upload cases. 109 matched expectations; the sole mismatch was F2. The 10 MiB and ten-file acceptance boundaries and one-byte/one-file excess rejections were exercised independently. Sanitized case results are in docs/acceptance/2026-09-10-http-matrix.md.
- JPG and JPEG were additionally uploaded through the browser; both protected downloads returned image/jpeg and exact matching bytes. Existing browser runs covered PNG and PDF. The seven-category export contained 17 edited submissions and four declarations with independently checked XML/cell values.
- Fresh local CA-verified HTTPS smoke completed submit/query/edit/review/XLSX; live SQLx reported 3.46.1, HTTP redirected with 308, prior persisted records remained accessible, and captured logs excluded credentials/private markers. Nine real automatic backups left seven pairs; restoring the latest pair and waiting for health preserved this round's reviewed record, original attachment and export via new sessions. Only isolated synthetic data was used.
- Non-blocking review notes: Home lacks a UTC deadline label; multipart buffers bounded files before disk persistence despite streaming wording; small CSS spacing/important-rule deviations remain. Public-domain TLS and a fresh office-suite GUI run are not claimed; earlier LibreOffice evidence is explicitly archived evidence for the unchanged export writer.
- IMPLEMENTATION_PLAN now carries outcomes and report links. Stale Task 2/3 checkboxes were reconciled with present implementation/tests; incomplete Task 4 requirements stay open. Historical red-first execution is not inferred from current green tests. Final report: docs/final-acceptance.md; confirmed lessons appended to lessons.md.

Local manual preview (2026-09-10):
- User requested an interactive local test. Started container zongce-local-preview in the existing isolated Colima profile zongce-task10, reachable at http://localhost:3000; administrator entry is /admin/login. Keep this preview/profile running while the user tests it. The old automatic-acceptance web/caddy containers were stopped.
- The unchanged application image uses SQLx SQLite 3.46.1 and development HTTP cookies, publishing only 127.0.0.1:3000. Persistent named volumes are zongce-local-preview-data and zongce-local-preview-uploads; restarting the preview retains submissions and attachments.
- Provisioned a separate 2026-2027 local-test academic year (2026-08-31 through 2027-08-30, active, no deadline) and class code through authenticated CSRF-protected settings forms. Generated local-test access credentials were shared privately in the conversation and are not recorded here.
- Verified /healthz, home/current-year display, administrator login/dashboard/logout, class-code access, submission form, CSS and JavaScript responses. No application code was changed, and F1/F2 from final acceptance remain open. Runtime provisioning files are temporary local artifacts; the preview runs independently in Docker after this session returns.
- User requested access for testers on the same Wi-Fi. Enabled a separate SSH forward bound specifically to the current en0 address 10.143.190.28:3000, retaining localhost:3000 and the existing container/sessions/data. LAN URL: http://10.143.190.28:3000. Verified host access through this address to health, home, CSS and Query; a separate physical-device connection was not exercised. The Mac firewall is disabled; Wi-Fi client isolation, if present, remains a network condition.
- LAN forwarding uses the isolated SSH control socket /tmp/zongce-local-preview.1hw1rG/lan-ssh.sock and Lima host alias lima-colima-zongce-task10. Keep it running while others test. Rebind if the Wi-Fi address changes; changing this forward does not require restarting the application.

Local feedback delivery and acceptance closure (2026-09-10):
- Completed user feedback in codex/ui-feedback: genuine declaration rows labeled 无申报材料; protected administrator modification-code display/manual reset; CET-4/CET-6 with old CET-6/cet6_score compatibility; restrained blue/white UI with clearer sections, choices, counts, filters and review panels. Submission counts, approved totals and fixed XLSX columns remain consistent. Implementation commits: cd15449, 243ad75, aa41e71; design/plan: da9814e.
- F1 is closed: no-JavaScript declaration/category/edit flows use an authenticated, CSRF-protected, non-persisting field refresh. JavaScript disables/hides fallback controls and makes them ordinary buttons so Enter submits normally. F2 is closed: school-honor category is optional and school-only, has all six presets and preserves historical custom values. D1 UTC label and D3 CSS spacing/important-rule deviations are also closed.
- New modification codes keep the Argon2id verifier plus authenticated encrypted recovery data derived from persistent SESSION_SECRET and bound to the submission. Legacy hash-only originals cannot be recovered; administrator reset requires explicit confirmation, CSRF and a version-checked POST. Reset invalidates old codes, scopes and outstanding receipts while preserving review/business fields and attachments. Upgrade never resets codes automatically. Key backup/rotation boundaries are documented in AGENT/PRD/APP_FLOW/BACKEND/README.
- Final fresh fmt, strict Clippy, cargo +1.88.0 test --all-targets (95 tests), both backup Python checks (eight fault scenarios), diff and Compose configuration checks passed. Exact final linux/amd64 image built with SQLx SQLite 3.46.1 gate. Scoped reviews and whole-change review found no remaining Important/Critical findings.
- Three actual-image browser suites ran sequentially and passed at 320x568, 390x844, 768x1024 and 1440x900. Coverage includes Enter submission, no-JS declaration/category/edit, administrator disclosure/reset/revocation and declaration rows; student/export suite produced 22 submissions and four declarations. Screenshots were inspected with no overflow/overlap. Local CA-verified HTTPS submit/query/edit/review/XLSX and log-privacy smoke passed.
- Independent actual-image migration/restart acceptance preserved an old approved record, declaration, hash and attachment; verified unavailable legacy originals, explicit reset protection, old-code/session revocation, unchanged business data/files, protected disclosure and recovery after restart. The unchanged D2 bounded Vec upload buffering is documented, not described as direct streaming-to-disk; no new public-domain TLS or office-suite GUI run is claimed.
- Updated the running manual preview to zongce-web:feedback only after a paired SQLite/uploads backup. Preserved its actual environment (including SESSION_SECRET), named data/uploads volumes and credentials. Cold-migration comparisons verified every original table column and attachment manifest unchanged. Kept zongce-local-preview-before-feedback stopped for rollback reference and the complete snapshot in zongce-local-preview-backups; no test records/settings were reset or reseeded.
- Current URLs: http://localhost:3000 and http://192.168.31.41:3000; administrator path /admin/login. Wi-Fi address changed, so the previous 10.143.190.28 forward was replaced. Both current health endpoints passed; separate physical-device access remains for user testing. Keep the preview/Colima/LAN forward running; refresh and log in again after the application restart.
- Root's pre-existing local/LAN operational notes above were preserved. Final plan/acceptance/lessons records are included in the delivery commit; existing origin/main is the authorized GitHub destination. The unrelated task1-scaffold worktree remains untouched.

Roster and deletion delivery (2026-09-10):
- User explicitly moved roster eligibility into scope: import names plus student numbers before creating a year, provide a template, allow additions after activation, and restrict result/declaration/update identity to that year's roster. Single-class scope is unchanged. All submission statuses and declarations support confirmed single/batch soft deletion and recovery; whole-year deletion requires full-name/checkbox confirmation and permanently removes that year's children and roster with durable attachment cleanup.
- Added migration 0004, protected admin routes/pages, bounded XLSX/CSV/TSV parsing, one-hour session drafts, atomic year/roster creation, per-year roster append, normal-query/export/authorization exclusions, and recovery. Production startup no longer seeds academic years; test fixtures/examples explicitly prepare their rosters. Existing 2025-2026 data is neither backfilled nor activated.
- New exact dependencies calamine 0.30.1, csv 1.3.1, quick-xml 0.37.5 and zip 4.6.1 support Rust 1.88.0; no new production runtime. ZIP expansion and sparse worksheet coordinates are bounded before allocation; text student numbers preserve leading zeros.
- Verification passed: cargo fmt --check; cargo test --all-targets (101); strict all-target/all-feature Clippy; git diff --check; Compose config --quiet; native administrator/settings/student browser regressions at all four sizes; new roster/recycle browser suite at all four sizes both natively and in the final linux/amd64 image. Tablet admin forms run without JavaScript. Screenshots were inspected. Release gate confirmed actual SQLx SQLite 3.46.1.
- Backed up current preview DB/uploads together and cold-verified every original table column/file digest before publication. Preserved credentials, Session Secret, settings and named volumes. Removed only old test year id=2 (3 results, 1 declaration, 3 attachments) through the implemented admin API; imported three synthetic students before recreating/activating the same test-year name with empty records. Year id=1 remained byte-for-byte equal at the row level. Both localhost:3000 and 192.168.31.41:3000 are healthy. Keep the preview and LAN forwarding running.
- Current image is zongce-web:roster-recycle. Previous container zongce-local-preview-before-roster is stopped for rollback reference; paired snapshots remain in zongce-local-preview-backups. Operation evidence lives in the private temporary roster-release directory; no credentials or personal data are recorded here. No GitHub publish was performed in this task.
- New local-test year id=3 is active with zero results/declarations/attachments. Pre-reset paired snapshot: snapshot-20260910T140951Z-PaUqjWkK in zongce-local-preview-backups. Updated general HTTP smoke passed on a disposable final-image instance after adding roster preparation; no extra test data was added to the user's preview. Temporary test instances were stopped, while the user preview/LAN forward remain running.

GitHub delivery notes (2026-09-11):
- User requested change notes followed by commit and push to the existing origin/main. This delivery packages the 56 changed/new files for roster eligibility, record recycling and permanent academic-year deletion.
- Cargo.toml/Cargo.lock and migration 0004 add the pinned import dependencies, per-year roster, soft-deletion fields and durable attachment-cleanup queue. The db/services/routes modules implement import, identity checks, atomic deletion/recovery and protected administrator actions; main/state/storage connect startup cleanup and attachment removal.
- Admin templates and app.css expose roster preparation/append, confirmed single/batch deletion, recovery and whole-year deletion. Examples, shared fixtures and Rust/browser/HTTP tests prepare eligible synthetic students and cover the new workflows.
- PRD, APP_FLOW, BACKEND_STRUCTURE, TECH_STACK, IMPLEMENTATION_PLAN, README, lessons and docs/testing-roster-recycle.md record the behavior, implementation constraints and prior acceptance evidence.
- Fresh pre-commit verification passed: cargo fmt --check; cargo test --locked --all-targets (101 tests); cargo clippy --locked --all-targets --all-features -- -D warnings; git diff --check; DOMAIN=example.invalid docker-compose --project-name zongce config --quiet --no-env-resolution. Compose used the installed standalone command and explicit project name, validating structure without resolving private runtime environment files. Prior browser/image acceptance remains documented above.
- Next step: continue manual testing of the existing roster/recycle preview; source publication does not require restarting it or changing its data.
